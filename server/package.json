{
    "name": "android-app-server",
    "version": "1.0.0",
    "description": "Local server for Android App",
    "main": "index.js",
    "scripts": {
        "start": "json-server --watch db.json --host 0.0.0.0 --port 3000"
    },
    "dependencies": {
        "json-server": "^0.17.4"
    }
}